# Projections

This tree stores derived projection outputs generated from accepted world state.

Projection surfaces are subordinate to accepted epoch state.
They must never outrank current canonical state files.
